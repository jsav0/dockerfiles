# default: on
# description: Gophernicus - Modern full-featured gopher server
service gopher
{
	socket_type	= stream
	wait		= no
	user		= nobody
	server		= /usr/local/sbin/gophernicus
	server_args	= -nf -nv -na -nt -nu 
	disable		= no
}
